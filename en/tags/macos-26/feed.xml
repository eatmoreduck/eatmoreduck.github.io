<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>MacOS 26 on eatmoreduck&#39;s Blog</title>
    <link>https://blog.xiaohuangyu.space/en/tags/macos-26/</link>
    <description>Recent content in MacOS 26 on eatmoreduck&#39;s Blog</description>
    <generator>Hugo</generator>
    <language>en</language>
    <lastBuildDate>Wed, 02 Sep 2026 11:26:31 +0800</lastBuildDate>
    <atom:link href="https://blog.xiaohuangyu.space/en/tags/macos-26/feed.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Block macOS 26 Updates (Still Works in 2026, Removes the Badge)</title>
      <link>https://blog.xiaohuangyu.space/en/p/block-macos-26-updates/</link>
      <pubDate>Mon, 29 Dec 2025 14:28:17 +0800</pubDate>
      <guid>https://blog.xiaohuangyu.space/en/p/block-macos-26-updates/</guid>
      <description>&lt;h2 id=&#34;why-block-updates-at-all&#34;&gt;Why block updates at all&lt;/h2&gt;&#xA;&lt;p&gt;After macOS 26 came out, the update nag kept popping up every few days. I kept worrying I&amp;rsquo;d slip and hit &amp;ldquo;Update Now&amp;rdquo;, confirmation or not. It got frequent enough that I wanted a fix that sticks.&lt;/p&gt;&#xA;&lt;p&gt;My reasons, plainly:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;The red badge drives me crazy&lt;/li&gt;&#xA;&lt;li&gt;Older machines don&amp;rsquo;t handle the new system well, and I don&amp;rsquo;t want the slowdown&lt;/li&gt;&#xA;&lt;li&gt;I need a stable work setup, and an upgrade breaking my dev environment is not worth the risk&lt;/li&gt;&#xA;&lt;li&gt;I dislike the liquid glass look in macOS 26, and some apps may not be compatible yet&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;So after upgrading to macOS 15.7.3, I decided to block macOS updates completely and get rid of the badge.&lt;/p&gt;</description>
      <content:encoded><![CDATA[<h2 id="why-block-updates-at-all">Why block updates at all</h2>
<p>After macOS 26 came out, the update nag kept popping up every few days. I kept worrying I&rsquo;d slip and hit &ldquo;Update Now&rdquo;, confirmation or not. It got frequent enough that I wanted a fix that sticks.</p>
<p>My reasons, plainly:</p>
<ul>
<li>The red badge drives me crazy</li>
<li>Older machines don&rsquo;t handle the new system well, and I don&rsquo;t want the slowdown</li>
<li>I need a stable work setup, and an upgrade breaking my dev environment is not worth the risk</li>
<li>I dislike the liquid glass look in macOS 26, and some apps may not be compatible yet</li>
</ul>
<p>So after upgrading to macOS 15.7.3, I decided to block macOS updates completely and get rid of the badge.</p>
<blockquote>
<p>The methods below still work on macOS 26 (Tahoe) in 2026, but honestly, the newer the system, the more stubborn the update nag is. Blocking hosts makes the download fail, but it doesn&rsquo;t always kill the badge cleanly. So I split this into two layers: hosts as a fallback, and a configuration profile for the real fix (the recommended, low-effort approach in 2026).</p>
</blockquote>
<h2 id="step-1-turn-off-automatic-updates-in-system-settings">Step 1: Turn off automatic updates in System Settings</h2>
<ol>
<li>Open System Settings → General → Software Update</li>
<li>Click the ⓘ icon on the right</li>
<li>Turn off &ldquo;Automatically keep my Mac up to date&rdquo;</li>
</ol>
<h2 id="step-2-block-updates-via-hosts">Step 2: Block updates via hosts</h2>
<p>This stops your Mac from reaching the macOS update servers. Edit the hosts file:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">sudo vim /etc/hosts
</span></span></code></pre></div><p>Append these lines:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">swscan</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">swdist</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">swdownload</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">swcdn</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">updates</span><span class="o">-</span><span class="n">http</span><span class="o">.</span><span class="n">cdn</span><span class="o">-</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">updates</span><span class="o">.</span><span class="n">cdn</span><span class="o">-</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">gdmf</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">xp</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span><span class="line"><span class="cl"><span class="mf">127.0</span><span class="o">.</span><span class="mf">0.1</span> <span class="n">mesu</span><span class="o">.</span><span class="n">apple</span><span class="o">.</span><span class="n">com</span>
</span></span></code></pre></div><p>Then flush the DNS cache:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">sudo dscacheutil -flushcache
</span></span><span class="line"><span class="cl">sudo killall -HUP mDNSResponder
</span></span></code></pre></div><h2 id="step-3-get-rid-of-the-red-badge">Step 3: Get rid of the red badge</h2>
<p>After the previous step, you&rsquo;ll still see the macOS 26 update prompt, but clicking update will fail. At that point, click update, and in the dialog that appears, <strong>do not click &ldquo;Check for Updates&rdquo;</strong>. Then press <code>command+option+esc</code> to open Force Quit, force quit System Settings, and the badge disappears.
<img loading="lazy" src="https://cdn.jsdelivr.net/gh/eatmoreduck/picture-repository@master/blog/20251229144404126.png" alt="Force quit System Settings to remove the update badge"  />
</p>
<h3 id="the-limits-of-the-hosts-approach">The limits of the hosts approach</h3>
<p>One thing to be honest about: from macOS 15 (Sequoia) through 26 (Tahoe), Apple has made update detection and notifications increasingly stubborn. The hosts block above stops the upgrade package from downloading, but the badge and the &ldquo;Upgrade to macOS Tahoe&rdquo; prompt can still reappear, especially after a background daemon does a scheduled check. If the badge keeps coming back every few days, skip to Step 4 and use a configuration profile. That&rsquo;s the clean fix.</p>
<h2 id="step-4-recommended-in-2026-use-a-configuration-profile-to-hide-the-upgrade">Step 4 (recommended in 2026): Use a configuration profile to hide the upgrade</h2>
<p>This is what the community (r/macsysadmin, Jamf, Daring Fireball, etc.) converged on in 2026, and honestly it&rsquo;s the least effort. The idea is to install a &ldquo;software update deferral&rdquo; profile so the macOS 26 upgrade entry simply disappears from the Software Update screen, badge included. You don&rsquo;t need enterprise MDM for a personal Mac. You can build a <code>.mobileconfig</code> yourself and install it.</p>
<p>The two keys that matter in the Restrictions (<code>com.apple.applicationaccess</code>) payload:</p>
<ul>
<li><code>forceDelayedMajorSoftwareUpdates = true</code> — hides the major version upgrade (i.e. macOS 26)</li>
<li><code>managedInstallDelay = 90</code> — the deferral in days, max 90</li>
</ul>
<h3 id="building-the-profile">Building the profile</h3>
<p>You don&rsquo;t need to hand-write XML. Use the free <strong>iMazing Profile Editor</strong> or <strong>Apple Configurator</strong> (on the App Store) to create a new profile, add General → Restrictions, enable forcing a delay on major software updates, set the days to 90, and export a <code>.mobileconfig</code> file.</p>
<h3 id="installing-the-profile">Installing the profile</h3>
<ol>
<li>Double-click the exported <code>.mobileconfig</code>; macOS tells you a profile was downloaded</li>
<li>Open System Settings → Privacy &amp; Security, scroll to the bottom and find &ldquo;Profiles&rdquo;</li>
<li>Select the profile, click &ldquo;Install&rdquo;, and enter your password when asked</li>
</ol>
<p>Go back to Software Update and the &ldquo;Upgrade to macOS Tahoe&rdquo; entry and the badge are gone. A lot less hassle than fighting hosts.</p>
<h3 id="things-to-keep-in-mind">Things to keep in mind</h3>
<ul>
<li><strong>90-day cap</strong>: the deferral maxes out at 90 days. Reinstall a fresh profile before it expires if you want to stay put long-term, so set a calendar reminder.</li>
<li><strong>Manual install limits</strong>: on macOS 26, a manually installed profile isn&rsquo;t as forceful as one from an MDM-enrolled device, but for hiding the major macOS upgrade it&rsquo;s enough</li>
<li><strong>When you do want to upgrade</strong>: remove the profile (in the Profiles screen, click &ldquo;Remove&rdquo;), wait a moment, and the update entry comes back</li>
</ul>
<h2 id="already-clicked-update-or-the-update-already-downloaded">Already clicked Update, or the update already downloaded?</h2>
<p>A few readers ran into this before finding the post: they slipped and clicked &ldquo;Update Now&rdquo;, or macOS quietly downloaded the update in the background. It&rsquo;s reversible, depending on how far it went.</p>
<h3 id="case-1-you-havent-restarted-yet">Case 1: You haven&rsquo;t restarted yet</h3>
<p>Go to System Settings → General → Software Update and click the ⓘ next to the downloading entry. On recent macOS (Ventura and later) you&rsquo;ll usually find a &ldquo;Delete Update&rdquo; button there — if it exists, use it. Cleanest option.</p>
<p>If it doesn&rsquo;t, the package is already sitting on disk: minor updates land in <code>/Library/Updates</code>, major upgrades in &ldquo;macOS Install Data&rdquo; at the root of your drive. Try deleting them from the terminal first:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">sudo rm -rf /Library/Updates/*
</span></span><span class="line"><span class="cl">sudo rm -rf <span class="s2">&#34;/macOS Install Data&#34;</span>
</span></span></code></pre></div><p>If you get <code>Operation not permitted</code>, that&rsquo;s not a typo — SIP is protecting those files. The workaround is clunkier: boot into Recovery (Apple Silicon: shut down, hold the power button until &ldquo;Loading startup options&rdquo; appears, click Options → Continue; Intel: hold Cmd+R at boot), open Terminal from the Utilities menu, run <code>csrutil disable</code>, reboot and run the delete commands again, then boot back into Recovery once more and run <code>csrutil enable</code> to turn SIP back on.</p>
<p>Afterwards, check Software Update to confirm nothing is pending, and make sure the Step 4 profile is installed (or your hosts entries are still in place), or it will quietly download everything again in a few days.</p>
<h3 id="case-2-you-already-restarted-and-it-installed">Case 2: You already restarted and it installed</h3>
<p>Once it&rsquo;s installed there is no &ldquo;undo&rdquo;. Your options are a full Time Machine restore or wiping the drive and reinstalling an older macOS, and both hurt. Which is exactly why you want the block in place before it gets that far.</p>
<h2 id="how-to-undo-this">How to undo this</h2>
<p>If you ever want to update again, depends on which method you used:</p>
<ul>
<li><strong>hosts approach</strong>: comment out or delete the relevant lines in <code>/etc/hosts</code>, flush DNS, and click Check for Updates again</li>
<li><strong>configuration profile approach</strong>: go to System Settings → Privacy &amp; Security → Profiles, remove the profile, and the macOS upgrade entry shows up again after a short wait</li>
</ul>
<h2 id="a-final-note">A final note</h2>
<p>I used to chase the latest version too. Every app, every OS, always current. After rebuilding my dev environment more times than I care to remember, I lost interest in the tinkering. Since I got an ARM Mac in 2021, something in my setup has broken roughly once every year or two, and every time I regret being too aggressive with updates.</p>
<p>This year I finally set up Time Machine and blocked updates for good. No more worrying about an accidental click.</p>
<p>This whole thing takes about five minutes. Worth it.</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
